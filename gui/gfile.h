#ifndef LEAF_GUI_GFILE_H
#define LEAF_GUI_GFILE_H

#include <string>

namespace leaf
{
struct gfile
{
    std::string parent;
    std::string filename;
    std::string type;
};
}    // namespace leaf
#endif
